# ATLAS Token — single-user local dashboard auth
# Generate a strong random string and keep it secret.
ATLAS_TOKEN=replace_this_with_a_strong_random_token

# Optional: override DB location (default: ./data/atlas.db)
# ATLAS_DB_PATH=
